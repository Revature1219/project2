export class Item {
    constructor(public id: number, public name: string, public price: number, public image: string,
                public details: string, public seller: string, public reviews: string) { }
}
